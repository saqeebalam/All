
spring.datasource.url = jdbc:mysql://localhost:3306/myblog?useSSL=false&serverTimezone=UTC
spring.datasource.username = root
spring.datasource.password = Saqeeb09@

# hibernate properties
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Hibernate ddl auto
spring.jpa.hibernate.ddl-auto = update
spring.mvc.pathmatch.matching-strategy = ANT_PATH_MATCHER

# Configure spring acutator here all actuator show
management.endpoints.web.exposure.include=*
#It show healt details all
management.endpoint.health.show-details=always
#it show base url is now admin
management.endpoints.web.base-path=/admin